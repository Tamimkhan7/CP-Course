#include <bits/stdc++.h>
using namespace std;
int main()
{
    // ai code ar time complexity koto, time complexity hobe O(1);
    // karon ai code just akbar kaj korbe a and b man joto hok na keno, tai tar time complexity hobe O(1)
    int a, b;
    cin >> a >> b;
    int sum = a + b;
    cout << sum << endl;
}